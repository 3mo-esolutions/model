{
	"name": "@3mo/sample-client",
	"version": "0.0.1",
	"scripts": {
		"start": "concurrently \"webpack serve --mode=development\" \"webpack --watch --env=test --mode=development\"",
		"lint": "eslint **/*.ts",
		"test": "karma start",
		"test:ci": "webpack --env=test --mode=development && karma start --single-run",
		"build": "webpack --mode=production"
	},
	"license": "Apache 2.0",
	"dependencies": {
		"@3mo/model": "file:../.."
	},
	"devDependencies": {
		"concurrently": "*",
		"favicons-webpack-plugin": "*",
		"lit-analyzer": "*",
		"standard-version": "*",
		"ts-lit-plugin": "*",
		"typescript": "4.*",
		"webpack": "5.*",
		"webpack-cli": "*",
		"webpack-dev-server": "*"
	}
}